to update image tag

we need to automate image tag in the deployment.yml file of k8s

dir command in jenkins, to change to current direcoty to give path

dir(path){

}
$BUILD_ID , default environmental variables in jenkins

----

stage('K8S Update Image Tag') {
when {
branch 'PR*'
}
steps {
sh 'git clone -b main http://64.227.187.25:5555/dasher-org/solar-system-gitops-argocd'
dir("solar-system-gitops-argocd/kubernetes") {
sh '''
#### Replace Docker Tag ####
git checkout main
git checkout -b feature-$BUILD_ID
sed -i "s#siddharth67.*#siddharth67/solar-system: $GIT_COMMIT#g" deployment.yml
cat deployment.yml
#### Commit and Push to Feature Branch ####
git config --global user.email "jenkins@dasher.com"
git remote set-url origin http:/$GITEA_TOKEN@64.227.187.25:5555/dasher-org/solar-system-
git add.
git commit -am "Updated docker image"
git push -u origin feature-$BUILD_ID
